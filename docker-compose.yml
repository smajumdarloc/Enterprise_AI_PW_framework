services:

  tests:

    build:
      context: .

    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}

    volumes:
      - ./allure-results:/app/allure-results
      - ./reports:/app/reports

    command:
      - npx
      - playwright
      - test